#include <stdio.h>

int main() {

    int m, n, soma;

    printf ("Digite dois numeros: \n");
    scanf ("%d", &n);
    scanf ("%d", &m);
    soma = n + m;
    printf ("A soma e de : %d", soma);

    return 0;
}
